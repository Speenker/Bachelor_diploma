[pytest]
testpaths = tests
addopts = -ra
markers =
	smoke: fast high-level checks for basic service health
	regression: stable suite to detect regressions after changes
	functional: extended functional suite with negative and edge-case scenarios
	slow: long-running tests excluded from default runs

# Root workspace ignores
# This file is for local artifacts and reference documents.

# Python virtual environments
.venv/
venv/
ENV/

# Python bytecode
__pycache__/
*.py[cod]

# Test/coverage/tool caches (if commands are run from repo root)
.pytest_cache/
.coverage
coverage.xml
htmlcov/
junit.xml
test-results/
reports/

.mypy_cache/
.ruff_cache/
.tox/

# VS Code settings (optional, keep local)
.vscode/

# Local helper scripts
extract_docx_text.py

# Reference documents and extracted text
*.doc
*.docx
*.docm
*.dot
*.dotx
*.dotm
*.docx.txt
*.doc.txt
*.docm.txt

# Local notes in repo root (do not ignore txt inside project subfolders)
/*.txt

# Supervisor progress notes (local)
прогресс и план.txt

# Optional: keep the Wekan source tree out of your diploma repository
wekan/

# Env files (if accidentally created at repo root)
.env

# Common OS artifacts
Thumbs.db
.DS_Store

# Common binaries (rarely needed in source control)
*.exe
*.exe~
*.dll
*.so
*.dylib
